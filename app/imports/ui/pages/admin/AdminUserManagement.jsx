import React from 'react';
import { withTracker } from 'meteor/react-meteor-data';

const AdminUserManagement = () => {
  return <p>Hello World</p>;
};

AdminUserManagement.propTypes = {};

export default withTracker(() => {})(AdminUserManagement);
